# Database
DATABASE_URL=postgres://aurora:aurora@postgres:5432/aurora
POSTGRES_USER=aurora
POSTGRES_PASSWORD=aurora
POSTGRES_DB=aurora

# Search (32+ characters when Meilisearch is enabled; init-env replaces GENERATE_ME)
MEILI_MASTER_KEY=GENERATE_ME

# Backend secrets (auto-generated by init-env.sh if left as GENERATE_ME)
JWT_SECRET=GENERATE_ME
SIGNING_SECRET=GENERATE_ME
MASTER_SECRET=GENERATE_ME

# Object storage (Nebular OS — https://github.com/AsP3X/nebular-os)
NOS_JWT_SECRET=GENERATE_ME
NOS_SIGNING_SECRET=GENERATE_ME
OBJECT_STORAGE_URL=http://object-storage:9000
OBJECT_STORAGE_PUBLIC_URL=http://localhost:9000
OBJECT_STORAGE_BUCKET=music
STORAGE_MODE=proxy
# Optional Nebular OS tuning (defaults are fine for Aurora)
# NOS_MAX_BODY_SIZE=104857600
# NOS_SOFT_DELETE_TTL_SECS=86400
# NOS_RECONCILE_ON_STARTUP=false

# Misc
URL_EXPIRY_SECONDS=3600
RUST_LOG=info
BIND_ADDR=0.0.0.0:3000
MUSIC_DIR=/music
